name: checker
inputs:
  environment:
    required: true

runs:
  using: "composite"
  env:
    VALUE: ${{ vars.VALUE }}
  steps:
    - name: checker check
      shell: bash
      run: |
        env
        echo value: $VALUE
        
