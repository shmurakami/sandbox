name: checker
inputs:
  environment:
    required: true

runs:
  using: "composite"
  environment: ${{ inputs.environment }}
  steps:
    - name: checker check
      shell: bash
      run: |
        echo value: ${{ vars.VALUE }}
