name: checker
inputs:
  environment:
    required: true

runs:
  using: "composite"
  steps:
    - name: checker check
      env:
        VALUE: ${{ vars.VALUE }}
      shell: bash
      run: |
        env
        echo value: $VALUE
        
